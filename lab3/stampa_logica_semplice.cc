#include <iostream>

using namespace std;

int main() {
    int a, b;

    cout << "Inserisci il valore di a: ";
    cin >> a;

    cout << "Inserisci il valore di b: ";
    cin >> b;
    
    cout << "a < b ? " << (a<b) << endl;

    return 0;
}
